"""YouTube Downloader — a console app to download videos, playlists, subtitles,
and split them into chapters.

The package is organized around single-responsibility modules and depends on the
abstract interfaces in ``interfaces`` (dependency inversion), with concrete
implementations wired together in ``main.py``.
"""
